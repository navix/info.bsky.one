---
const { src, ar, wide, alt } = Astro.props;
---

<img src={src} alt={alt} style={{'aspect-ratio': ar, 'max-width': wide ? 'min(48rem, 100%)' : '32rem'}}/>

<style>
  img {
    width: 100%;
    margin: 1rem auto;
    border: 1px solid #777;
    padding: 2px;
    border-radius: 2px;
  }
</style>
